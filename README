环境需要 xmake、gcc-14、protoc

确保 xmake.lua 中的 protobuf-cpp 版本和 protoc 版本一致。

bug 触发流程：

```shell
xmake r master
xmake r storage
xmake r client
:connect 127.0.0.1 8889
```

如果没有触发，尝试多次连接，或者重新运行 storage 再连接。
